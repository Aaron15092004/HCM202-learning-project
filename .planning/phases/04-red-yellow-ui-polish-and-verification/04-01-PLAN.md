---
phase: 04-red-yellow-ui-polish-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-mln-learning-main/src/styles/theme.css
  - my-mln-learning-main/src/main.jsx
  - my-mln-learning-main/src/components/common/BackHomeButton.jsx
  - my-mln-learning-main/src/pages/Courses.jsx
  - my-mln-learning-main/src/pages/FlipCardPage.jsx
  - my-mln-learning-main/src/pages/QuizPage.jsx
  - my-mln-learning-main/src/pages/QuizResultsPage.jsx
  - my-mln-learning-main/src/pages/Tuvipage.jsx
  - my-mln-learning-main/src/components/home/MainContent.jsx
  - my-mln-learning-main/src/components/home/Footer.jsx
  - my-mln-learning-main/src/styles/home/home.css
  - my-mln-learning-main/src/styles/courses/courses.css
  - my-mln-learning-main/src/styles/quiz/quiz.css
  - my-mln-learning-main/src/styles/flip/flip.css
  - my-mln-learning-main/src/styles/tuvi/TuviPage.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - ROUT-01
  - ROUT-02
  - VERI-01
  - VERI-02
  - VERI-03
user_setup: []
must_haves:
  truths:
    - The system theme uses a coherent Vietnamese red-yellow palette across home, courses, quiz, quiz results, flip, and the retained `/tuvi` standalone page.
    - Blue/purple legacy accents are removed from active HCM202 surfaces except where they are part of third-party library internals or inaccessible generated/vendor CSS.
    - Back-to-home controls use one shared React component or one shared class contract across courses, quiz results, flip, and `/tuvi`.
    - `/tuvi` remains routable because the user explicitly asked not to delete it, but it is not promoted in the HCM202 primary navigation.
    - Typography and sizing changes must prevent overflow/overlap on desktop and mobile without scaling font size with viewport width.
    - Verification must include lint, build, route checks, and visual smoke screenshots for desktop and mobile.
  artifacts:
    - my-mln-learning-main/src/styles/theme.css
    - my-mln-learning-main/src/components/common/BackHomeButton.jsx
    - .planning/phases/04-red-yellow-ui-polish-and-verification/04-01-SUMMARY.md
    - .planning/phases/04-red-yellow-ui-polish-and-verification/04-VERIFICATION.md
  key_links:
    - `main.jsx` currently imports framework CSS only; importing a local `theme.css` there gives all routes access to shared variables.
    - `HomeLayout.jsx` imports home CSS, `QuizLayout.jsx` imports quiz CSS, and standalone pages import their own CSS; shared tokens should avoid duplicating palette values.
    - `Courses.jsx` and `FlipCardPage.jsx` already use `.back-home-btn`, while `Tuvipage.jsx` uses a separate `.back-home` button and `QuizResultsPage.jsx` uses inline button styles.
    - `/tuvi` is intentionally retained in `App.jsx`; route visibility should be handled in navigation/content, not by deleting the route.
---

<objective>
Finish the HCM202 conversion by applying a coherent red-yellow UI system, synchronizing navigation/back controls, normalizing typography, and verifying the app across active routes.

Purpose: after this phase, the app should feel like one finished HCM202 learning product rather than a converted collection of old blue/purple templates.

Output: shared theme tokens, page-level red-yellow UI polish, consistent back buttons, responsive text/layout fixes, route/build verification, and documented residual warnings.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-project-identity-conversion/01-VERIFICATION.md
@.planning/phases/02-course-and-quiz-data-replacement/02-01-SUMMARY.md
@.planning/phases/02-course-and-quiz-data-replacement/02-VERIFICATION.md
@.planning/phases/03-ho-chi-minh-assets-and-flip-cards/03-01-SUMMARY.md
@.planning/phases/03-ho-chi-minh-assets-and-flip-cards/03-VERIFICATION.md
@my-mln-learning-main/src/main.jsx
@my-mln-learning-main/src/App.jsx
@my-mln-learning-main/src/layouts/HomeLayout.jsx
@my-mln-learning-main/src/layouts/QuizLayout.jsx
@my-mln-learning-main/src/components/home/Header.jsx
@my-mln-learning-main/src/components/home/MainContent.jsx
@my-mln-learning-main/src/components/home/Footer.jsx
@my-mln-learning-main/src/pages/Courses.jsx
@my-mln-learning-main/src/pages/QuizPage.jsx
@my-mln-learning-main/src/pages/QuizResultsPage.jsx
@my-mln-learning-main/src/pages/FlipCardPage.jsx
@my-mln-learning-main/src/pages/Tuvipage.jsx
@my-mln-learning-main/src/styles/home/home.css
@my-mln-learning-main/src/styles/courses/courses.css
@my-mln-learning-main/src/styles/quiz/quiz.css
@my-mln-learning-main/src/styles/flip/flip.css
@my-mln-learning-main/src/styles/tuvi/TuviPage.css
</context>

<scope>
Phase 4 owns:
- Shared red-yellow color tokens and route-level visual polish.
- Button/link/active state color conversion.
- Back button synchronization across pages.
- Typography, button, card, quiz option, and responsive overflow fixes.
- Route scope verification for retained `/tuvi` and HCM202 navigation.
- Final lint/build/route/browser-style verification documentation.

Non-goals:
- Do not delete `/tuvi`.
- Do not rewrite the jQuery flip-card game.
- Do not change course/quiz academic content unless a UI label has stale wording.
- Do not rebuild the app or introduce a design-system dependency.
- Do not solve unrelated build warnings from vendor/template CSS unless they block this phase.
</scope>

<tasks>

<task type="auto">
  <name>Create shared HCM202 theme tokens and apply the palette across route CSS</name>
  <files>my-mln-learning-main/src/styles/theme.css, my-mln-learning-main/src/main.jsx, my-mln-learning-main/src/styles/home/home.css, my-mln-learning-main/src/styles/courses/courses.css, my-mln-learning-main/src/styles/quiz/quiz.css, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/styles/tuvi/TuviPage.css, my-mln-learning-main/src/components/home/MainContent.jsx, my-mln-learning-main/src/components/home/Footer.jsx, my-mln-learning-main/src/pages/Courses.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, my-mln-learning-main/src/pages/Tuvipage.jsx</files>
  <read_first>my-mln-learning-main/src/main.jsx, my-mln-learning-main/src/styles/home/home.css, my-mln-learning-main/src/styles/courses/courses.css, my-mln-learning-main/src/styles/quiz/quiz.css, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/styles/tuvi/TuviPage.css, my-mln-learning-main/src/components/home/MainContent.jsx, my-mln-learning-main/src/components/home/Footer.jsx, my-mln-learning-main/src/pages/Courses.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, my-mln-learning-main/src/pages/Tuvipage.jsx</read_first>
  <action>
Add `src/styles/theme.css` and import it from `main.jsx` after third-party CSS imports. Define stable tokens such as:
- `--hcm-red`, `--hcm-red-dark`, `--hcm-gold`, `--hcm-gold-soft`, `--hcm-ink`, `--hcm-muted`, `--hcm-surface`, `--hcm-border`, `--hcm-shadow`, `--hcm-gradient-primary`, `--hcm-gradient-soft`.

Use tokens to replace the dominant old blue/purple accents in active route surfaces:
- `courses.css`: replace `#00b4d8`, `#3626b2`, and matching rgba shadows with red/gold values. Keep course page structure.
- `quiz.css`: replace blue/purple gradients in timer, question title, active options, progress, next/start buttons, modal title/focus states. Improve mobile spacing where labels or question text can overflow.
- `QuizResultsPage.jsx`: move inline style string toward red/gold and make card/button sizes responsive.
- `home.css`, `MainContent.jsx`, and `Footer.jsx`: replace obvious blue/purple inline CSS variables and visible accents with theme tokens or red/gold literal values where inline style objects require strings.
- `flip.css`: keep Phase 3 red/gold work, but align values with shared tokens where practical and remove remaining blue/purple decorative borders if visually obvious.
- `TuviPage.css` and `Tuvipage.jsx`: do not make `/tuvi` an HCM202 content page, but align its back/home control and primary action accents enough that it no longer feels visually detached when reached directly.

Use direct CSS variables rather than broad global overrides that might accidentally break Bootstrap or third-party image zoom styles. Avoid one-note flat red screens: pair red/gold with white/cream surfaces and dark ink text for readability.
  </action>
  <verify>node -e "const fs=require('fs'); const required=['--hcm-red','--hcm-red-dark','--hcm-gold','--hcm-gradient-primary','--hcm-surface']; const text=fs.readFileSync('my-mln-learning-main/src/styles/theme.css','utf8'); for(const token of required){ if(!text.includes(token)) throw new Error('missing theme token '+token); } const main=fs.readFileSync('my-mln-learning-main/src/main.jsx','utf8'); if(!main.includes('./styles/theme.css')&&!main.includes('./styles/theme.css')) throw new Error('theme.css not imported from main.jsx');"; node -e "const fs=require('fs'); const files=['my-mln-learning-main/src/styles/courses/courses.css','my-mln-learning-main/src/styles/quiz/quiz.css','my-mln-learning-main/src/pages/QuizResultsPage.jsx','my-mln-learning-main/src/components/home/MainContent.jsx','my-mln-learning-main/src/components/home/Footer.jsx','my-mln-learning-main/src/styles/tuvi/TuviPage.css','my-mln-learning-main/src/pages/Tuvipage.jsx']; const banned=/#3626b2|#00b4d8|0, 180, 216|54, 38, 178|#667eea|#764ba2|#23153c|#2185d0/i; for(const file of files){const text=fs.readFileSync(file,'utf8'); if(banned.test(text)) throw new Error('legacy blue/purple accent remains in '+file); }"</verify>
  <acceptance_criteria>
    - A shared theme file exists and is imported once.
    - Courses, quiz, quiz results, home accents, flip, and `/tuvi` no longer visually rely on old blue/purple accents.
    - Red/gold colors maintain readable contrast against white/cream/dark surfaces.
    - No route logic is rewritten while applying theme changes.
  </acceptance_criteria>
  <done>Shared tokens and route CSS are aligned with the HCM202 red-yellow visual system.</done>
</task>

<task type="auto">
  <name>Synchronize back-home controls across pages without removing `/tuvi`</name>
  <files>my-mln-learning-main/src/components/common/BackHomeButton.jsx, my-mln-learning-main/src/pages/Courses.jsx, my-mln-learning-main/src/pages/FlipCardPage.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, my-mln-learning-main/src/pages/Tuvipage.jsx, my-mln-learning-main/src/styles/theme.css, my-mln-learning-main/src/styles/courses/courses.css, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/styles/tuvi/TuviPage.css</files>
  <read_first>my-mln-learning-main/src/pages/Courses.jsx, my-mln-learning-main/src/pages/FlipCardPage.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, my-mln-learning-main/src/pages/Tuvipage.jsx, my-mln-learning-main/src/styles/courses/courses.css, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/styles/tuvi/TuviPage.css</read_first>
  <action>
Create a small shared `BackHomeButton.jsx` component under `src/components/common/` unless an equivalent shared component already exists. It should:
- Render a `Link` to `/` by default.
- Use a house/back icon already available in Bootstrap icons classes.
- Expose optional `className`, `label`, and `variant` props only if needed by existing pages.
- Use a stable shared class such as `hcm-back-home-btn`.

Replace duplicated back/home controls in:
- `Courses.jsx`
- `FlipCardPage.jsx`
- `QuizResultsPage.jsx` if it has a home action button that should match the navigation pattern.
- `Tuvipage.jsx`, preserving `/tuvi` content and route.

Update route CSS so placement is consistent:
- Top-left fixed/absolute for full-screen specialty pages (`flip`, `/tuvi`).
- Top-left inside the page header for `courses`.
- Result page can use the shared component in the action row or as a top-left control, but it must match icon/text/hover treatment.

Keep mobile behavior consistent: show icon-only when width is too tight, preserve an accessible label (`aria-label`), and prevent button text from wrapping awkwardly.
  </action>
  <verify>node -e "const fs=require('fs'); const component='my-mln-learning-main/src/components/common/BackHomeButton.jsx'; if(!fs.existsSync(component)) throw new Error('missing BackHomeButton component'); const text=fs.readFileSync(component,'utf8'); for(const s of ['Link','hcm-back-home-btn','Trang chủ']){ if(!text.includes(s)) throw new Error('BackHomeButton missing '+s); } const pages=['my-mln-learning-main/src/pages/Courses.jsx','my-mln-learning-main/src/pages/FlipCardPage.jsx','my-mln-learning-main/src/pages/QuizResultsPage.jsx','my-mln-learning-main/src/pages/Tuvipage.jsx']; for(const page of pages){ const src=fs.readFileSync(page,'utf8'); if(!src.includes('BackHomeButton')) throw new Error('page does not use shared BackHomeButton: '+page); }"; node -e "const fs=require('fs'); const files=['my-mln-learning-main/src/pages/Courses.jsx','my-mln-learning-main/src/pages/FlipCardPage.jsx','my-mln-learning-main/src/pages/Tuvipage.jsx']; const old=/className=\"back-home-btn\"|className=\"back-home\"|Back Home/; for(const file of files){ const text=fs.readFileSync(file,'utf8'); if(old.test(text)) throw new Error('old back button pattern remains in '+file); }"</verify>
  <acceptance_criteria>
    - Back/home controls use one shared component or one shared class contract.
    - Visible label is consistently Vietnamese: `Trang chủ`.
    - Mobile button behavior is consistent and does not overflow.
    - `/tuvi` route remains present in `App.jsx`.
  </acceptance_criteria>
  <done>Back navigation is visually and structurally synchronized across pages.</done>
</task>

<task type="auto">
  <name>Normalize typography, spacing, and responsive behavior on major workflows</name>
  <files>my-mln-learning-main/src/styles/home/home.css, my-mln-learning-main/src/styles/courses/courses.css, my-mln-learning-main/src/styles/quiz/quiz.css, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/styles/tuvi/TuviPage.css, my-mln-learning-main/src/pages/QuizPage.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, my-mln-learning-main/src/pages/Courses.jsx</files>
  <read_first>my-mln-learning-main/src/styles/home/home.css, my-mln-learning-main/src/styles/courses/courses.css, my-mln-learning-main/src/styles/quiz/quiz.css, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/styles/tuvi/TuviPage.css, my-mln-learning-main/src/pages/QuizPage.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, my-mln-learning-main/src/pages/Courses.jsx</read_first>
  <action>
Audit desktop and mobile layout risks and apply scoped fixes:
- Course page: header text, topic select, content card, quote box, and mindmap card must fit at 360px width. Avoid absolute back button overlap with title.
- Quiz config modal: title, selects, and start button must fit at 360px width without horizontal scroll.
- Quiz active question view: long question text and answer labels should wrap naturally, use `overflow-wrap`, and avoid huge fixed margins on mobile.
- Quiz results: score/title/buttons must fit at 360px width and remain readable.
- Flip page: start screen title and back button should not overlap at 360px width; keep card grid stable.
- `/tuvi`: shared back control should not cover masthead content on small screens.

Prefer `max-width`, `clamp()` with fixed min/max, `overflow-wrap: anywhere` for unpredictable text, and grid/flex wrapping. Do not scale font size directly with viewport width. Avoid nested cards or broad structural rewrites.
  </action>
  <verify>node -e "const fs=require('fs'); const files=['my-mln-learning-main/src/styles/courses/courses.css','my-mln-learning-main/src/styles/quiz/quiz.css','my-mln-learning-main/src/styles/flip/flip.css','my-mln-learning-main/src/styles/tuvi/TuviPage.css']; const combined=files.map(f=>fs.readFileSync(f,'utf8')).join('\\n'); for(const needle of ['overflow-wrap','@media','max-width']){ if(!combined.includes(needle)) throw new Error('responsive CSS missing '+needle); } if(/font-size:\\s*[^;]*vw/i.test(combined)) throw new Error('viewport-scaled font-size found');"</verify>
  <acceptance_criteria>
    - Major route content fits at common mobile widths without horizontal scrolling caused by app CSS.
    - Long quiz/course text wraps cleanly.
    - Buttons maintain stable dimensions and readable labels.
    - No global CSS change unintentionally restyles all third-party components.
  </acceptance_criteria>
  <done>Typography and responsive behavior are normalized enough for final v1 verification.</done>
</task>

<task type="auto">
  <name>Verify navigation scope, routes, build, and visual smoke</name>
  <files>.planning/phases/04-red-yellow-ui-polish-and-verification/04-01-SUMMARY.md, .planning/phases/04-red-yellow-ui-polish-and-verification/04-VERIFICATION.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>my-mln-learning-main/src/App.jsx, my-mln-learning-main/src/components/home/Header.jsx, my-mln-learning-main/src/components/home/MainContent.jsx, my-mln-learning-main/src/components/home/Footer.jsx, .planning/phases/03-ho-chi-minh-assets-and-flip-cards/03-VERIFICATION.md</read_first>
  <action>
Run final automated checks and document the result.

Route scope:
- Confirm `App.jsx` still defines `/tuvi`.
- Confirm primary HCM202 navigation/header/footer does not promote `/tuvi`.
- Confirm active HCM202 routes `/`, `/courses`, `/quiz`, `/quiz/results`, and `/flip` remain reachable. For `/quiz/results`, use a direct history-state browser script or document route fallback behavior if direct load intentionally shows zero score.

Verification:
- Run stale color scan from earlier tasks.
- Run `npm --prefix my-mln-learning-main run lint`.
- Run `npm --prefix my-mln-learning-main run build`.
- Start a strict local dev server on a free port and HTTP-check `/`, `/courses`, `/quiz`, `/quiz/results`, `/flip`, and `/tuvi`.
- Use Playwright CLI or the in-app Browser if available to capture desktop and mobile screenshots for `/`, `/courses`, `/quiz`, `/flip`, and `/tuvi`. If Browser tooling is unavailable, use Playwright CLI screenshots and document the limitation.
- Perform at least one quiz smoke path if practical: start quiz, answer one question, navigate next/submit path enough to confirm controls respond. If full interaction is blocked by tooling, document exact blocker and rely on route/build plus manual UAT checklist.

Create:
- `04-01-SUMMARY.md` with what changed and known warnings.
- `04-VERIFICATION.md` with pass/fail table for UI-01..05, ROUT-01..02, VERI-01..03.

Update planning docs:
- Mark UI-01..05, ROUT-01..02, VERI-01..03 complete only if verification supports it.
- Update `.planning/STATE.md` to milestone/phase complete state if all checks pass.
- Update `.planning/PROJECT.md` Validated/Active sections for v1 completion.
  </action>
  <verify>npm --prefix my-mln-learning-main run lint; npm --prefix my-mln-learning-main run build; node -e "const fs=require('fs'); const app=fs.readFileSync('my-mln-learning-main/src/App.jsx','utf8'); if(!app.includes('path=\"/tuvi\"')) throw new Error('/tuvi route missing'); const navFiles=['my-mln-learning-main/src/components/home/Header.jsx','my-mln-learning-main/src/components/home/MainContent.jsx','my-mln-learning-main/src/components/home/Footer.jsx']; for(const file of navFiles){ const text=fs.readFileSync(file,'utf8'); if(/to=[\"']\\/tuvi[\"']|href=[\"']\\/tuvi[\"']/i.test(text)) throw new Error('/tuvi promoted in primary navigation: '+file); }"; $port=5184; try { (Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port/" -TimeoutSec 2) | Out-Null; throw "port $port already has a responding server; choose a free test port" } catch { if($_.Exception.Message -like "port $port already*"){throw} }; $proc=Start-Process -FilePath 'npm.cmd' -ArgumentList '--prefix','my-mln-learning-main','run','dev','--','--host','127.0.0.1','--port',"$port",'--strictPort' -WorkingDirectory (Get-Location) -PassThru -WindowStyle Hidden; try { $ready=$false; for($i=0;$i -lt 30;$i++){try{(Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port/" -TimeoutSec 2) | Out-Null; $ready=$true; break}catch{Start-Sleep -Seconds 1}}; if(-not $ready -or $proc.HasExited){throw 'dev server did not become ready on the owned strict port'}; foreach($route in '/','/courses','/quiz','/quiz/results','/flip','/tuvi'){ $status=(Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port$route" -TimeoutSec 5).StatusCode; if($status -ne 200){throw "unexpected status $status for $route"} } } finally { Stop-Process -Id $proc.Id -Force -ErrorAction SilentlyContinue }</verify>
  <acceptance_criteria>
    - Lint and build pass or exact warnings are documented.
    - Routes `/`, `/courses`, `/quiz`, `/quiz/results`, `/flip`, and `/tuvi` respond locally.
    - `/tuvi` is retained but not promoted from primary HCM202 navigation.
    - Screenshots or documented visual checks cover desktop and mobile.
    - Phase planning docs accurately record completion status and residual risks.
  </acceptance_criteria>
  <done>Phase 4 verification is complete and documented.</done>
</task>

</tasks>

<risks>
- `home.css` contains a large inherited template stylesheet with many old variables and unresolved template URLs. Phase 4 should fix active visible accents, not attempt a risky full vendor-template purge unless a stale reference is visibly user-facing.
- `QuizResultsPage.jsx` currently defines CSS in a template string; conversion can be done in place, but moving to a CSS file is acceptable if it stays scoped and simple.
- Browser plugin availability has been inconsistent. Playwright CLI screenshots are an acceptable fallback if Browser tool is unavailable.
- Existing lint warnings in `QuizPage.jsx` and `Tuvipage.jsx` may be unrelated. Fix them only if straightforward and safe; otherwise document them under VERI-01.
- `/tuvi` is unrelated to HCM202 but user explicitly said not to delete it. Treat it as retained standalone route, not a primary learning route.
</risks>

<verification>
Before declaring this plan complete:
- [ ] Shared theme file exists and is imported by `main.jsx`.
- [ ] Active blue/purple scan passes for the targeted app-owned files.
- [ ] Back/home controls use `BackHomeButton` or the shared `hcm-back-home-btn` contract.
- [ ] `/tuvi` remains in `App.jsx`.
- [ ] `/tuvi` is not linked from primary HCM202 navigation.
- [ ] `npm --prefix my-mln-learning-main run lint`
- [ ] `npm --prefix my-mln-learning-main run build`
- [ ] HTTP route checks pass for `/`, `/courses`, `/quiz`, `/quiz/results`, `/flip`, and `/tuvi`.
- [ ] Desktop and mobile screenshots/visual smoke are captured or exact tooling limitation is documented.
- [ ] `04-01-SUMMARY.md` and `04-VERIFICATION.md` are created.
</verification>

<success_criteria>
- UI-01 through UI-05 are implemented by shared red-yellow tokens, route-level theme polish, synchronized controls, and responsive text/layout fixes.
- ROUT-01 and ROUT-02 are implemented by retaining `/tuvi`, excluding it from HCM202 navigation, and verifying route availability.
- VERI-01 through VERI-03 are implemented by lint/build/route checks and documented visual smoke.
- The converted project is ready for milestone review or final cleanup after Phase 4 execution.
</success_criteria>

<output>
After execution, create `.planning/phases/04-red-yellow-ui-polish-and-verification/04-01-SUMMARY.md` and `.planning/phases/04-red-yellow-ui-polish-and-verification/04-VERIFICATION.md`.
</output>
