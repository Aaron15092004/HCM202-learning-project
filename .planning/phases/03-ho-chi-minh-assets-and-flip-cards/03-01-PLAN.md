---
phase: 03-ho-chi-minh-assets-and-flip-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-mln-learning-main/src/assets/images/makdahome.png
  - my-mln-learning-main/src/assets/images/thu-ha.png
  - my-mln-learning-main/src/assets/images/demo_certificates.png
  - my-mln-learning-main/src/assets/images/logo/logo.png
  - my-mln-learning-main/src/assets/images/logo/logo-dark2.svg
  - my-mln-learning-main/public/thIcon.jpg
  - my-mln-learning-main/public/chu-de-1.jpg
  - my-mln-learning-main/public/chu-de-2.jpg
  - my-mln-learning-main/public/chu-de-3.jpg
  - my-mln-learning-main/public/hcm202-flip/
  - my-mln-learning-main/src/components/home/MainContent.jsx
  - my-mln-learning-main/src/data/flipImagesData.js
  - my-mln-learning-main/src/pages/FlipCardPage.jsx
  - my-mln-learning-main/src/styles/flip/flip.css
autonomous: true
requirements:
  - FLIP-01
  - FLIP-02
  - FLIP-03
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
user_setup: []
must_haves:
  truths:
    - `makdahome.png` and `thu-ha.png` are replaced with radiant Ho Chi Minh themed imagery in the requested vivid red-yellow style.
    - Logo, favicon, and public topic images no longer imply the old teacher/project identity.
    - `flipImagesData.js` uses local HCM202 / Ho Chi Minh themed image paths, not old philosophy/Marxism-Leninism hotlinks or remote URLs.
    - Flip-card data remains compatible with the existing jQuery/React memory game; hard mode still has enough distinct images for 32 pairs.
    - Flip-card images fit the generated card backs without obvious distortion or awkward crop on desktop and mobile.
    - Phase 3 does not remove `/tuvi` and does not perform the full red-yellow UI system overhaul reserved for Phase 4.
  artifacts:
    - my-mln-learning-main/src/assets/images/makdahome.png
    - my-mln-learning-main/src/assets/images/thu-ha.png
    - my-mln-learning-main/src/assets/images/demo_certificates.png
    - my-mln-learning-main/src/assets/images/logo/logo.png
    - my-mln-learning-main/src/assets/images/logo/logo-dark2.svg
    - my-mln-learning-main/public/thIcon.jpg
    - my-mln-learning-main/public/chu-de-1.jpg
    - my-mln-learning-main/public/chu-de-2.jpg
    - my-mln-learning-main/public/chu-de-3.jpg
    - my-mln-learning-main/public/hcm202-flip/
    - my-mln-learning-main/src/components/home/MainContent.jsx
    - my-mln-learning-main/src/data/flipImagesData.js
    - my-mln-learning-main/src/pages/FlipCardPage.jsx
    - my-mln-learning-main/src/styles/flip/flip.css
  key_links:
    - `MainContent.jsx` imports `makdahome.png` and `thu-ha.png`; replacing those files updates home media without JSX rewrites.
    - `index.html` references `/thIcon.jpg`; replacing the public file updates the favicon path without metadata rewrites.
    - `coursesData.js` uses `chu-de-1.jpg`, `chu-de-2.jpg`, and `chu-de-3.jpg`; replacing public files preserves Phase 2 data.
    - `FlipCardPage.jsx` reads `flipImages` and applies each string as CSS `background-image`; local public paths are the safest format.
---

<objective>
Replace legacy visual assets and flip-card image data with radiant Ho Chi Minh themed imagery that fits the existing app surfaces.

Purpose: after Phase 3, the app no longer depends on old teacher imagery, old public topic images, old logo/favicon signals, or remote philosophy/Marxism-Leninism flip images. The flip-card game should remain playable with correctly sized local HCM202 imagery.

Output: replaced bitmap/SVG assets, local flip-card image set, updated `flipImagesData.js`, and minimal flip layout/CSS adjustments needed for fit.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
@C:/Users/Admin/.codex/skills/.system/imagegen/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-project-identity-conversion/01-VERIFICATION.md
@.planning/phases/02-course-and-quiz-data-replacement/02-01-SUMMARY.md
@.planning/phases/02-course-and-quiz-data-replacement/02-VERIFICATION.md
@my-mln-learning-main/src/components/home/MainContent.jsx
@my-mln-learning-main/src/components/home/Header.jsx
@my-mln-learning-main/index.html
@my-mln-learning-main/src/data/coursesData.js
@my-mln-learning-main/src/data/flipImagesData.js
@my-mln-learning-main/src/pages/FlipCardPage.jsx
@my-mln-learning-main/src/styles/flip/flip.css
</context>

<scope>
Phase 3 owns:
- Replacing `makdahome.png` and `thu-ha.png` with bright red-yellow glowing Ho Chi Minh imagery.
- Replacing logo/favicon/public topic images so visible image references no longer signal the old project.
- Replacing `flipImagesData.js` with local HCM202 image paths.
- Creating enough local flip-card image assets for hard mode.
- Adjusting flip-card image fit and only the local flip-game styling needed to avoid crop/distortion.

Non-goals:
- Do not rewrite the flip-card game from jQuery to React.
- Do not replace course or quiz content; Phase 2 already completed active course/quiz data.
- Do not perform the whole red-yellow theme conversion across all pages; Phase 4 owns system theme, typography, and button synchronization.
- Do not remove `/tuvi`; it remains available by user request.
- Do not add a backend, image CDN, or runtime network dependency for learning/flip images.
</scope>

<tasks>

<task type="auto">
  <name>Generate and replace primary Ho Chi Minh visual assets</name>
  <files>my-mln-learning-main/src/assets/images/makdahome.png, my-mln-learning-main/src/assets/images/thu-ha.png, my-mln-learning-main/src/assets/images/demo_certificates.png, my-mln-learning-main/src/assets/images/logo/logo.png, my-mln-learning-main/src/assets/images/logo/logo-dark2.svg, my-mln-learning-main/public/thIcon.jpg, my-mln-learning-main/public/chu-de-1.jpg, my-mln-learning-main/public/chu-de-2.jpg, my-mln-learning-main/public/chu-de-3.jpg, my-mln-learning-main/src/components/home/MainContent.jsx</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, my-mln-learning-main/src/components/home/MainContent.jsx, my-mln-learning-main/src/components/home/Header.jsx, my-mln-learning-main/index.html, my-mln-learning-main/src/data/coursesData.js</read_first>
  <action>
Use the `imagegen` skill to create bitmap assets in a consistent style: respectful Ho Chi Minh themed educational imagery, vivid Vietnamese red and gold palette, warm radiant glow, polished and bright, not dark or stock-like. Keep imagery appropriate for a study app and avoid old teacher branding.

Replace these files in place:
- `src/assets/images/makdahome.png`: large hero/home image. Current file is 2464x2658; generate a portrait-oriented PNG with similar or intentionally compatible dimensions and enough transparent/quiet edge space to fit the existing home composition.
- `src/assets/images/thu-ha.png`: secondary home image. Current file is 1252x1280; generate a square/near-square PNG with Ho Chi Minh themed red-yellow glow.
- `public/chu-de-1.jpg`, `public/chu-de-2.jpg`, `public/chu-de-3.jpg`: topic/mind-map placeholders. Generate three HCM202 topic images, landscape or wide educational panels compatible with the courses page zoom display. They can be visual chapter covers rather than literal mind maps.
- `public/thIcon.jpg`: 512x512 favicon/app icon using HCM202 red-yellow identity, no old teacher/project imagery.
- `src/assets/images/logo/logo.png`: small raster logo compatible with quiz header usage. Keep it legible at the current small display size.
- `src/assets/images/demo_certificates.png`: replace the old generic certificate graphic with a local HCM202 study/badge/supporting graphic or remove its visible usage if it is no longer useful.

Update `src/assets/images/logo/logo-dark2.svg` directly as a simple HCM202 Learning red-yellow SVG logo if that is cleaner than generating a bitmap. Preserve import compatibility in `Header.jsx`.

Update `MainContent.jsx` so visible home image references use local HCM202 assets instead of the old Moodle/template/demo imagery. Replace or remove active `data-src`, `data-bg`, `src`, `backgroundImage`, placeholder, and alt text references for any `lmsstyle.com/theme/new-learning` asset, including `lazy_placeholder`, `lazy_placeholder_big`, `bg_shape_yellow`, `pluginfile`, `demo_logos`, `cloud-playgrounds`, `professional-certificates`, and `demo_certificates`. Keep route links and HCM202 copy intact.
  </action>
  <verify>Add-Type -AssemblyName System.Drawing; $checks=@(@{File='my-mln-learning-main/src/assets/images/makdahome.png';MinW=900;MinH=900;MinRatio=0.55;MaxRatio=1.8},@{File='my-mln-learning-main/src/assets/images/thu-ha.png';MinW=700;MinH=700;MinRatio=0.75;MaxRatio=1.35},@{File='my-mln-learning-main/src/assets/images/demo_certificates.png';MinW=256;MinH=128;MinRatio=0.8;MaxRatio=4.0},@{File='my-mln-learning-main/public/chu-de-1.jpg';MinW=900;MinH=500;MinRatio=1.1;MaxRatio=2.2},@{File='my-mln-learning-main/public/chu-de-2.jpg';MinW=900;MinH=500;MinRatio=1.1;MaxRatio=2.2},@{File='my-mln-learning-main/public/chu-de-3.jpg';MinW=900;MinH=500;MinRatio=1.1;MaxRatio=2.2},@{File='my-mln-learning-main/src/assets/images/logo/logo.png';MinW=128;MinH=48;MinRatio=1.5;MaxRatio=4.5}); foreach($c in $checks){$img=[System.Drawing.Image]::FromFile((Resolve-Path $c.File)); $ratio=$img.Width/$img.Height; if($img.Width -lt $c.MinW -or $img.Height -lt $c.MinH -or $ratio -lt $c.MinRatio -or $ratio -gt $c.MaxRatio){throw "bad image dimensions/aspect: $($c.File) ${($img.Width)}x${($img.Height)} ratio=$ratio"}; $img.Dispose()}; $icon=[System.Drawing.Image]::FromFile((Resolve-Path 'my-mln-learning-main/public/thIcon.jpg')); if($icon.Width -ne 512 -or $icon.Height -ne 512){throw 'thIcon.jpg must be exactly 512x512'}; $icon.Dispose(); node -e "const fs=require('fs'); const files=['my-mln-learning-main/src/assets/images/logo/logo-dark2.svg','my-mln-learning-main/src/components/home/MainContent.jsx']; const re=/TAHA|Thu Hà|Triết học Mác|Mác - Lênin|Marx|Engels|Lenin|Lênin|Premium Moodle|New Learning|lmsstyle\\.com\\/theme\\/new-learning|lazy_placeholder|bg_shape_yellow|pluginfile|demo_logos|cloud-playgrounds|professional-certificates|demo_certificates/i; for(const file of files){const text=fs.readFileSync(file,'utf8'); if(re.test(text)) throw new Error('old visual/template reference still present in '+file);}"</verify>
  <acceptance_criteria>
    - `makdahome.png` and `thu-ha.png` visibly depict the new Ho Chi Minh themed red-yellow radiant direction.
    - Public topic images are replaced with HCM202-appropriate visuals.
    - Favicon and logos no longer imply the old project or teacher.
    - Visible home image references no longer point to old lmsstyle/template/demo assets.
    - Existing imports and paths continue to work; only minimal `MainContent.jsx` image-reference edits are made.
  </acceptance_criteria>
  <done>Primary assets are replaced in place and remain loadable by existing JSX/data paths.</done>
</task>

<task type="auto">
  <name>Create local flip-card image set and replace flip data</name>
  <files>my-mln-learning-main/public/hcm202-flip/*, my-mln-learning-main/src/data/flipImagesData.js</files>
  <read_first>my-mln-learning-main/src/data/flipImagesData.js, my-mln-learning-main/src/pages/FlipCardPage.jsx, my-mln-learning-main/src/styles/flip/flip.css, .planning/phases/02-course-and-quiz-data-replacement/02-01-SUMMARY.md</read_first>
  <action>
Create a local public flip-card image directory such as `my-mln-learning-main/public/hcm202-flip/`.

Generate or derive 32 square or card-safe images for the memory game. Use consistent red-yellow HCM202 styling and avoid tiny unreadable text. The images may be simple visual cards such as: Ho Chi Minh portrait variants, lotus, book, star, flag, stilt house, pen, torch, dove/peace symbol, map silhouette, youth study, unity hands, cultural drum, and abstract glowing topic symbols. Each image should have centered subjects and generous margins so it works even when the game card is rectangular.

Rewrite `flipImagesData.js` to export exactly 32 local paths, for example `/hcm202-flip/card-01.png` through `/hcm202-flip/card-32.png`. Remove every remote URL and every old philosophy/Marxism-Leninism/Buddhist/Lenin image reference.

Keep the export shape as `export const flipImages = [ ... ]` because `FlipCardPage.jsx` imports it directly.
  </action>
  <verify>node -e "import('./my-mln-learning-main/src/data/flipImagesData.js').then(({flipImages})=>{const fs=require('fs'); const path=require('path'); if(flipImages.length!==32) throw new Error('expected 32 flip images'); for(const [i,img] of flipImages.entries()){if(!img.startsWith('/hcm202-flip/')||/^https?:/.test(img)) throw new Error('bad flip image path '+i+': '+img); const file=path.join('my-mln-learning-main/public', img.replace(/^\\//,'')); if(!fs.existsSync(file)) throw new Error('missing flip file '+file);}})"; Add-Type -AssemblyName System.Drawing; node -e "import('./my-mln-learning-main/src/data/flipImagesData.js').then(({flipImages})=>console.log(flipImages.join('\\n'))) " | ForEach-Object { $file=Join-Path 'my-mln-learning-main/public' ($_.TrimStart('/')); $img=[System.Drawing.Image]::FromFile((Resolve-Path $file)); $ratio=$img.Width/$img.Height; if($img.Width -lt 512 -or $img.Height -lt 512 -or $ratio -lt 0.9 -or $ratio -gt 1.1){throw "bad flip image dimensions/aspect: $file $($img.Width)x$($img.Height)"}; $img.Dispose() }; node -e "const fs=require('fs'); const text=fs.readFileSync('my-mln-learning-main/src/data/flipImagesData.js','utf8'); const re=/https?:|Triết học|Mác|Marx|Engels|Lenin|Lênin|Buddha|Phật|Socrates|philosophy/i; if(re.test(text)) throw new Error('old or remote flip image reference still present');"</verify>
  <acceptance_criteria>
    - `flipImagesData.js` contains exactly 32 local public paths.
    - Hard mode has enough unique image entries for 32 generated pairs.
    - No remote URLs remain in active flip image data.
    - No old philosophy/Marxism-Leninism/non-HCM image references remain in active flip image data.
    - Local image files exist for every path in the exported array.
  </acceptance_criteria>
  <done>Flip-card data uses a complete local HCM202 image set compatible with the existing game logic.</done>
</task>

<task type="auto">
  <name>Adjust flip-card image fit and verify asset routes</name>
  <files>my-mln-learning-main/src/pages/FlipCardPage.jsx, my-mln-learning-main/src/styles/flip/flip.css</files>
  <read_first>my-mln-learning-main/src/pages/FlipCardPage.jsx, my-mln-learning-main/src/styles/flip/flip.css, my-mln-learning-main/src/data/flipImagesData.js</read_first>
  <action>
Keep the existing jQuery game logic intact, but make the smallest fit-oriented changes needed so local images render cleanly. Prefer CSS/class changes over rewriting game behavior.

If generated images are square/card-safe, keep `background-position: center` and set `background-size` to `cover` only if no meaningful content is cropped. If cropping is visible or likely, change the card-back image style to use `contain`, a red-gold background color, and no distortion. Ensure card backs still hide/show correctly and matched cards still compare by `data-img`.

Optionally add a small visual polish to flip-only colors so the flip screen no longer reads blue/purple, but do not attempt the full app theme overhaul. Leave system-wide buttons/back buttons for Phase 4.

Run lint/build and local route checks. If Browser automation is still unavailable, document that limitation in summary and verification rather than blocking the plan.
  </action>
  <verify>npm --prefix my-mln-learning-main run lint; npm --prefix my-mln-learning-main run build; $port=5174; try { (Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port/" -TimeoutSec 2) | Out-Null; throw "port $port already has a responding server; choose a free test port" } catch { if($_.Exception.Message -like "port $port already*"){throw} }; $proc=Start-Process -FilePath 'npm.cmd' -ArgumentList '--prefix','my-mln-learning-main','run','dev','--','--host','127.0.0.1','--port',"$port",'--strictPort' -WorkingDirectory (Get-Location) -PassThru -WindowStyle Hidden; try { $ready=$false; for($i=0;$i -lt 30;$i++){try{(Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port/" -TimeoutSec 2) | Out-Null; $ready=$true; break}catch{Start-Sleep -Seconds 1}}; if(-not $ready -or $proc.HasExited){throw 'dev server did not become ready on the owned strict port'}; foreach($route in '/','/courses','/flip'){ $status=(Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$port$route" -TimeoutSec 5).StatusCode; if($status -ne 200){throw "unexpected status $status for $route"} } } finally { Stop-Process -Id $proc.Id -Force -ErrorAction SilentlyContinue }</verify>
  <acceptance_criteria>
    - `/flip` remains routable and the memory game can use all difficulty levels from the existing `data-level` values.
    - Card backs use local HCM202 images and fit the card area without obvious distortion.
    - Existing localStorage keys, stats, pause/ESC behavior, matching logic, and difficulty modes remain intact.
    - `npm run lint` and `npm run build` pass or exact known warnings/failures are documented.
  </acceptance_criteria>
  <done>Flip-card page is still playable with local HCM202 assets and fit-safe card backs.</done>
</task>

</tasks>

<risks>
- Browser automation has failed in this environment in prior phases. If it still fails, rely on image dimension checks, build/lint, HTTP route checks, and create a human UAT checklist for visual fit.
- Generated images may have inconsistent dimensions. Use explicit resize/crop/padding after generation so home assets, icons, topic images, and flip cards fit their different surfaces.
- `logo-dark2.svg` is imported as SVG; replacing it with invalid SVG would break the header. Validate it remains parseable text and Vite build succeeds.
- Flip hard mode uses `data-level="32"` and duplicates 32 distinct image indexes. Fewer than 32 exported images will cause repeated fallbacks and poorer gameplay.
- Full red-yellow theme is Phase 4; only adjust flip CSS enough to support asset fit and avoid legacy blue/purple dominance on the flip screen.
</risks>

<verification>
Before declaring this plan complete:
- [ ] Image dimension check passes for `makdahome.png`, `thu-ha.png`, `logo.png`, `thIcon.jpg`, and `chu-de-1..3.jpg`.
- [ ] `flipImagesData.js` exports exactly 32 local `/hcm202-flip/` paths.
- [ ] Every exported flip image file exists under `public/hcm202-flip/`.
- [ ] Stale flip data check finds no remote URLs or old philosophy/Marxism-Leninism/non-HCM references.
- [ ] `npm --prefix my-mln-learning-main run lint`
- [ ] `npm --prefix my-mln-learning-main run build`
- [ ] HTTP/browser route checks cover `/`, `/courses`, and `/flip`; interactive flip game is played through at least easy mode or documented as human-needed if Browser automation is unavailable.
</verification>

<success_criteria>
- FLIP-01, FLIP-02, and FLIP-03 are each implemented by local HCM202 flip assets, fit-safe rendering, and preserved game logic.
- ASST-01, ASST-02, ASST-03, and ASST-04 are each implemented by replacing home, logo/favicon, and public topic images with loadable HCM202 imagery.
- The app no longer depends on legacy remote flip images or old teacher/project visual assets in active HCM202 surfaces.
- Phase 3 leaves the project ready for Phase 4 system-wide red-yellow UI polish.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ho-chi-minh-assets-and-flip-cards/03-01-SUMMARY.md`.
</output>
