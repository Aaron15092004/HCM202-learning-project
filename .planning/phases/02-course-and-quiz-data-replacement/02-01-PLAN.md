---
phase: 02-course-and-quiz-data-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-mln-learning-main/src/data/coursesData.js
  - my-mln-learning-main/src/data/quizData.js
  - my-mln-learning-main/src/pages/Courses.jsx
  - my-mln-learning-main/src/pages/QuizPage.jsx
  - my-mln-learning-main/src/pages/QuizResultsPage.jsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
  - QUIZ-04
user_setup: []
must_haves:
  truths:
    - `coursesData` contains exactly seven generated HCM202 / "Tu tuong Ho Chi Minh" topics and no active Marxism-Leninism philosophy topic set.
    - Each course topic keeps the existing editable static shape used by `Courses.jsx`: `id`, `tieu_de`, `cau_noi_hay`, `mindmap_url`, and `cac_van_de` entries with `stt`, `van_de`, `noi_dung`.
    - `quizQuestions` contains only HCM202 / Ho Chi Minh Thought practice questions and covers all seven course topic areas.
    - Quiz data keeps the existing active shape used by `QuizPage.jsx`: numeric `id`, numeric `chapter`, `question`, four `options`, zero-based `correctAnswer`, and numeric `difficulty`.
    - Course selection, quiz start/filter/shuffle/timer/progress/results/retry navigation remain functional after data replacement.
    - Generated academic content is treated as editable scaffolding; the user may correct facts later by editing static data only.
  artifacts:
    - my-mln-learning-main/src/data/coursesData.js
    - my-mln-learning-main/src/data/quizData.js
    - my-mln-learning-main/src/pages/Courses.jsx
    - my-mln-learning-main/src/pages/QuizPage.jsx
    - my-mln-learning-main/src/pages/QuizResultsPage.jsx
  key_links:
    - `Courses.jsx` must render all seven `coursesData` entries without component logic rewrites unless a small label/empty-state adjustment is required.
    - `QuizPage.jsx` derives chapter options from `quizQuestions`, so Phase 2 quiz data chapters must be `1` through `7`.
    - Phase 3 owns image replacement; Phase 2 may keep placeholder `mindmap_url` values compatible with current public assets or intentionally blank fields if the UI fallback is acceptable.
---

<objective>
Replace the old Marxism-Leninism course and quiz datasets with a seven-topic HCM202 / "Tu tuong Ho Chi Minh" content scaffold.

Purpose: after Phase 2, students can study the generated HCM202 topic structure and practice matching quiz questions without old subject data leaking into the active course or quiz experience.

Output: updated static course and quiz data, plus only the minimal page label or guard adjustments needed to keep `/courses`, `/quiz`, and `/quiz/results` working.
</objective>

<execution_context>
@C:/Users/Admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-project-identity-conversion/01-01-SUMMARY.md
@.planning/phases/01-project-identity-conversion/01-VERIFICATION.md
@my-mln-learning-main/src/data/coursesData.js
@my-mln-learning-main/src/data/quizData.js
@my-mln-learning-main/src/pages/Courses.jsx
@my-mln-learning-main/src/pages/QuizPage.jsx
@my-mln-learning-main/src/pages/QuizResultsPage.jsx
</context>

<scope>
Phase 2 owns:
- Replacing every active course topic with seven HCM202 topics.
- Replacing every active quiz question with HCM202 practice questions.
- Keeping the course/quiz page logic compatible with the new data.
- Verifying old Marxism-Leninism/philosophy content is gone from the active course and quiz data files.

Non-goals:
- Do not replace `flipImagesData`; Phase 3 owns flip-card image data.
- Do not generate or replace Ho Chi Minh image assets; Phase 3 owns `makdahome.png`, `thu-ha.png`, logos, favicon, and public imagery.
- Do not perform the full red-yellow visual overhaul; Phase 4 owns system theme, typography, and button synchronization.
- Do not remove the `/tuvi` route. It remains available by user request and is outside Phase 2 unless a test command needs to confirm routes still build.
- Do not introduce a backend, CMS, markdown parser, or new content abstraction. Static ES module data is enough for this conversion.
</scope>

<tasks>

<task type="auto">
  <name>Replace course data with seven HCM202 topics</name>
  <files>my-mln-learning-main/src/data/coursesData.js, my-mln-learning-main/src/pages/Courses.jsx</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, my-mln-learning-main/src/data/coursesData.js, my-mln-learning-main/src/pages/Courses.jsx</read_first>
  <action>
Rewrite `coursesData` so it exports exactly seven entries for "Tu tuong Ho Chi Minh". Keep the existing field names and array/object shape so `Courses.jsx` can continue rendering without a broad component rewrite.

Use these seven topic areas as the generated scaffold:
1. Khai quat mon hoc Tu tuong Ho Chi Minh
2. Co so hinh thanh tu tuong Ho Chi Minh
3. Tu tuong Ho Chi Minh ve doc lap dan toc va chu nghia xa hoi
4. Tu tuong Ho Chi Minh ve Dang Cong san Viet Nam va Nha nuoc cua dan
5. Tu tuong Ho Chi Minh ve dai doan ket dan toc va doan ket quoc te
6. Tu tuong Ho Chi Minh ve van hoa, dao duc va con nguoi
7. Gia tri, van dung va phat trien tu tuong Ho Chi Minh trong hien nay

For each topic, provide:
- `id`: stable values such as `hcm01` through `hcm07`.
- `tieu_de`: Vietnamese display title.
- `cau_noi_hay`: concise quote-style study prompt or well-known Ho Chi Minh-oriented idea. Avoid overclaiming exact citations if unsure; these are editable scaffolding.
- `mindmap_url`: keep compatible strings such as `chu-de-1.jpg`, `chu-de-2.jpg`, and `chu-de-3.jpg`, or leave empty only if `Courses.jsx` fallback is preferred. Do not add new image assets in this phase.
- `cac_van_de`: 4 to 6 concise learning issues per topic using the existing `stt`, `van_de`, `noi_dung` shape.

Only adjust `Courses.jsx` if the new data needs small copy changes, guards, or better fallback labels. Do not change the topic selection model unless the existing shape breaks.
  </action>
  <verify>node -e "import('./my-mln-learning-main/src/data/coursesData.js').then(({coursesData})=>{if(coursesData.length!==7) throw new Error('expected 7 topics'); for(const [i,t] of coursesData.entries()){if(!t.id||!t.tieu_de||!t.cau_noi_hay||typeof t.mindmap_url!=='string'||!Array.isArray(t.cac_van_de)||t.cac_van_de.length<4) throw new Error('bad topic '+i); for(const [j,v] of t.cac_van_de.entries()){if(!Number.isInteger(v.stt)||!v.van_de||!v.noi_dung) throw new Error('bad issue '+i+'.'+j);}}})"; node -e "const fs=require('fs'); const text=fs.readFileSync('my-mln-learning-main/src/data/coursesData.js','utf8'); const re=/Mác - Lênin|Marx|Engels|duy vật biện chứng|duy vật lịch sử|phủ định của phủ định|lượng - chất|quy luật mâu thuẫn/i; if(re.test(text)) throw new Error('old course content still present');"</verify>
  <acceptance_criteria>
    - `coursesData` has exactly seven topics.
    - Every topic has the existing editable structure and at least four learning issues.
    - Old philosophy/Marxism-Leninism course topics are removed from active course data.
    - `/courses` can render the empty state and each selected topic with no runtime shape mismatch.
  </acceptance_criteria>
  <done>Course data is a seven-topic HCM202 scaffold compatible with the current course page.</done>
</task>

<task type="auto">
  <name>Replace quiz data with HCM202 practice questions</name>
  <files>my-mln-learning-main/src/data/quizData.js, my-mln-learning-main/src/pages/QuizPage.jsx</files>
  <read_first>my-mln-learning-main/src/data/quizData.js, my-mln-learning-main/src/pages/QuizPage.jsx</read_first>
  <action>
Rewrite `quizQuestions` so every active question practices HCM202 / "Tu tuong Ho Chi Minh" content. Keep the existing object shape exactly compatible with `QuizPage.jsx`.

Create a balanced set with at least 70 total questions, preferably 10 per topic/chapter. Use `chapter` values `1` through `7` so the quiz modal maps directly to the seven course topics. Use `difficulty` values `0`, `1`, and `2` with a useful spread for easy/medium/hard practice. Each question must have exactly four answer options and a zero-based `correctAnswer` index from 0 to 3.

Content should cover the same seven topic areas used in `coursesData`. The user already approved generated content as structure first, so prioritize clear editable scaffolding over exhaustive academic precision. Do not leave old quiz comments or old question blocks in the active exported array.

If needed, make small `QuizPage.jsx` label changes so chapter labels read as HCM202 "Chuyen de" labels, but keep filtering, shuffling, timer, selected answer state, and navigation behavior unchanged.
  </action>
  <verify>node -e "import('./my-mln-learning-main/src/data/quizData.js').then(({quizQuestions})=>{if(quizQuestions.length<70) throw new Error('expected >=70 questions'); const chapters=new Set(); const ids=new Set(); for(const [i,q] of quizQuestions.entries()){chapters.add(q.chapter); if(ids.has(q.id)) throw new Error('duplicate id '+q.id); ids.add(q.id); if(!Number.isInteger(q.id)||!Number.isInteger(q.chapter)||q.chapter<1||q.chapter>7||!q.question||!Array.isArray(q.options)||q.options.length!==4||!q.options.every(Boolean)||!Number.isInteger(q.correctAnswer)||q.correctAnswer<0||q.correctAnswer>3||!Number.isInteger(q.difficulty)||q.difficulty<0||q.difficulty>2) throw new Error('bad question '+i);} for(let c=1;c<=7;c++){if(!chapters.has(c)) throw new Error('missing chapter '+c);}})"; node -e "const fs=require('fs'); const text=fs.readFileSync('my-mln-learning-main/src/data/quizData.js','utf8'); const re=/Triết học Mác|Mác - Lênin|Marx|Engels|duy vật biện chứng|duy vật lịch sử|phủ định của phủ định|lượng - chất|quy luật mâu thuẫn/i; if(re.test(text)) throw new Error('old quiz content still present');"</verify>
  <acceptance_criteria>
    - `quizQuestions` contains at least 70 HCM202 questions.
    - Chapters 1 through 7 are all represented.
    - Every question has exactly four options, a valid zero-based answer index, and a numeric difficulty.
    - Old Marxism-Leninism/philosophy question content is removed from the active exported array.
    - Quiz config can filter by every chapter and difficulty without emptying all useful options unexpectedly.
  </acceptance_criteria>
  <done>Quiz data is fully replaced with HCM202 practice questions while preserving quiz logic compatibility.</done>
</task>

<task type="auto">
  <name>Verify course and quiz flows after data replacement</name>
  <files>my-mln-learning-main/src/data/coursesData.js, my-mln-learning-main/src/data/quizData.js, my-mln-learning-main/src/pages/Courses.jsx, my-mln-learning-main/src/pages/QuizPage.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx</files>
  <read_first>my-mln-learning-main/src/pages/Courses.jsx, my-mln-learning-main/src/pages/QuizPage.jsx, my-mln-learning-main/src/pages/QuizResultsPage.jsx, .planning/phases/01-project-identity-conversion/01-VERIFICATION.md</read_first>
  <action>
Run source-level data shape checks, stale-data greps, lint, and build. Start or reuse the Vite dev server if route checks are needed. Verify HTTP 200 for `/courses`, `/quiz`, and `/quiz/results`; route-state details for `/quiz/results` may still show the existing fallback when opened directly, but it must not crash.

If browser automation is available, manually exercise:
- `/courses`: choose each of the seven topics from the dropdown.
- `/quiz`: start a random quiz, select answers, move previous/next, submit, and confirm `/quiz/results` receives score state.

If browser automation is unavailable, document the blocker exactly and include automated HTTP/build/lint evidence in the summary.
  </action>
  <verify>npm --prefix my-mln-learning-main run lint; npm --prefix my-mln-learning-main run build; node -e "const fs=require('fs'); const files=['my-mln-learning-main/src/data/coursesData.js','my-mln-learning-main/src/data/quizData.js']; const re=/Triết học Mác|Mác - Lênin|Marx|Engels|duy vật biện chứng|duy vật lịch sử|phủ định của phủ định|lượng - chất|quy luật mâu thuẫn/i; for(const file of files){const text=fs.readFileSync(file,'utf8'); if(re.test(text)) throw new Error('old subject data still present in '+file);}"</verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 or exact warnings/failures are documented.
    - `npm run build` exits 0 or exact failures are documented.
    - Automated data shape checks pass.
    - Stale old-subject grep finds no active course/quiz data leaks, except any intentionally documented false positives if unavoidable.
    - A summary records whether browser/manual route interaction was completed or blocked.
  </acceptance_criteria>
  <done>Phase 2 data replacement is verified and ready for Phase 3 asset/flip work.</done>
</task>

</tasks>

<risks>
- Generated academic material may be factually imperfect. The user explicitly accepted generated structure first, so keep content easy to edit in static data files and avoid hiding facts in page logic.
- `QuizPage.jsx` derives chapters from numeric `chapter` fields. Non-numeric chapter labels would break sorting/filtering, so keep `chapter` numeric and update only visible labels if needed.
- Some stale grep terms such as "Lênin" may appear legitimately in historical context if generated content discusses sources of Ho Chi Minh Thought. Prefer avoiding those strings in Phase 2 scaffolding unless necessary; document any unavoidable false positives.
- Directly visiting `/quiz/results` uses fallback zero score behavior from the existing app. Do not redesign result persistence in this phase.
- `chu-de-1.jpg` through `chu-de-3.jpg` are old public images until Phase 3. Phase 2 can keep compatible placeholders but must not claim asset replacement is complete.
</risks>

<verification>
Before declaring this plan complete:
- [ ] `node -e "import('./my-mln-learning-main/src/data/coursesData.js').then(({coursesData})=>{if(coursesData.length!==7) throw new Error('expected 7 topics'); for(const [i,t] of coursesData.entries()){if(!t.id||!t.tieu_de||!t.cau_noi_hay||typeof t.mindmap_url!=='string'||!Array.isArray(t.cac_van_de)||t.cac_van_de.length<4) throw new Error('bad topic '+i); for(const [j,v] of t.cac_van_de.entries()){if(!Number.isInteger(v.stt)||!v.van_de||!v.noi_dung) throw new Error('bad issue '+i+'.'+j);}}})"`
- [ ] `node -e "import('./my-mln-learning-main/src/data/quizData.js').then(({quizQuestions})=>{if(quizQuestions.length<70) throw new Error('expected >=70 questions'); const chapters=new Set(); const ids=new Set(); for(const [i,q] of quizQuestions.entries()){chapters.add(q.chapter); if(ids.has(q.id)) throw new Error('duplicate id '+q.id); ids.add(q.id); if(!Number.isInteger(q.id)||!Number.isInteger(q.chapter)||q.chapter<1||q.chapter>7||!q.question||!Array.isArray(q.options)||q.options.length!==4||!q.options.every(Boolean)||!Number.isInteger(q.correctAnswer)||q.correctAnswer<0||q.correctAnswer>3||!Number.isInteger(q.difficulty)||q.difficulty<0||q.difficulty>2) throw new Error('bad question '+i);} for(let c=1;c<=7;c++){if(!chapters.has(c)) throw new Error('missing chapter '+c);}})"`
- [ ] `npm --prefix my-mln-learning-main run lint`
- [ ] `npm --prefix my-mln-learning-main run build`
- [ ] `node -e "const fs=require('fs'); const files=['my-mln-learning-main/src/data/coursesData.js','my-mln-learning-main/src/data/quizData.js']; const re=/Triết học Mác|Mác - Lênin|Marx|Engels|duy vật biện chứng|duy vật lịch sử|phủ định của phủ định|lượng - chất|quy luật mâu thuẫn/i; for(const file of files){const text=fs.readFileSync(file,'utf8'); if(re.test(text)) throw new Error('old subject data still present in '+file);}"` passes with no old-subject matches.
- [ ] HTTP/browser route checks cover `/courses`, `/quiz`, and `/quiz/results`; interactive quiz submit is completed manually or documented as human-needed.
</verification>

<success_criteria>
- CONT-01, CONT-02, CONT-03, and CONT-04 are each implemented by course data replacement while preserving editable static data.
- QUIZ-01, QUIZ-02, QUIZ-03, and QUIZ-04 are each implemented by quiz data replacement and quiz flow verification.
- No old Marxism-Leninism/philosophy course or quiz data remains in the active exported datasets.
- The converted course and quiz experience stays runnable after data replacement.
</success_criteria>

<output>
After completion, create `.planning/phases/02-course-and-quiz-data-replacement/02-01-SUMMARY.md`.
</output>
